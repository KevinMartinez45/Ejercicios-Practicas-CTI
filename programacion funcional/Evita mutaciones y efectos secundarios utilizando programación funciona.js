// La variable global
let fixedValue = 4;

function incrementer() {
  // Cambia solo el código debajo de esta línea
  let incrementValue = fixedValue + 1;
  return incrementValue;

  // Cambia solo el código encima de esta línea
}